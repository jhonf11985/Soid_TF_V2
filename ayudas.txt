========================================
COMANDOS BÁSICOS - PROYECTO Soid_TF_V2
Windows / PowerShell
========================================

# 📂 0. IR A LA CARPETA DEL PROYECTO
# (ajusta la ruta si lo tienes en otro sitio)

cd "C:\Users\jhonf\Desktop\Soid_Tf_v2\Soid_TF_V2"


# 🧪 1. CREAR ENTORNO VIRTUAL (SOLO LA PRIMERA VEZ)
# Esto solo se hace cuando el proyecto NO tiene carpeta venv.

python -m venv venv


# ✅ 2. ACTIVAR EL ENTORNO VIRTUAL (CADA VEZ QUE VAYAS A TRABAJAR)
# Debes ver algo como (venv) al inicio de la línea cuando funcione.


python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
python manage.py runserver


en Mac 

python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
python manage.py runserver

python manage.py shell

hacer merge

git push origin staging

git checkout main
git pull origin main

git checkout staging
git pull origin staging

git checkout main
git merge staging
git push origin main



http://127.0.0.1:8000/accounts/login/



#236a6a

#5c9ead